window.addEventListener("load", ()=>{
    let page = document.getElementById("page_name").value.split(";")
    menu = page[0]
    subMenu = page[1]
    .
    if(menu)
            if(subMenu=="list")
      
});